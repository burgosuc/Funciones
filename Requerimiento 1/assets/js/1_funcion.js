/*Transforma esta declaración de función a una función de expresión.

function example(a, b, c){
return a+b+c
}*/

//Transformación a expresion de funcion
const example = function (a, b, c) {
  return a + b + c;
}

//Para llamar la función se utiliza
resultado = example(3,4,3);

//Si se quiere mostrar o imprimir en la consola
console.log(example); //Mostrara como resultado 10.